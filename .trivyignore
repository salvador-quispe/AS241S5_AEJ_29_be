# .trivyignore
# CVEs sin fix disponible en la rama Spring Boot 3.5.x actualmente publicada.
# Revisar periódicamente si ya tienen parche publicado.

# ── spring-boot (fix: 3.5.14 — no publicado aún) ─────────────────────────────
# Arbitrary Code Execution via predictable temp file names (DevTools)
# Solo afecta si spring-boot-devtools está en classpath de producción (no es nuestro caso)
CVE-2026-40973

# ── logback (ya corregido con logback.version=1.5.25) ────────────────────────
CVE-2025-11226
CVE-2026-1225
