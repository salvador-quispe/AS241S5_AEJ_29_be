# .trivyignore
# CVEs sin fix disponible en la imagen base gcr.io/distroless/java21-debian12.
# Todos marcados como "will_not_fix" o "affected" sin parche upstream.
# Revisar periódicamente si ya tienen parche publicado.

# ── glibc / libc6 (Debian 12 - sin fix upstream) ────────────────────────────
CVE-2010-4756
CVE-2018-20796
CVE-2019-1010022
CVE-2019-1010023
CVE-2019-1010024
CVE-2019-1010025
CVE-2019-9192

# ── libssl / openssl (Debian 12 - sin fix disponible) ───────────────────────
CVE-2023-39368
CVE-2023-23914
CVE-2023-23915
CVE-2023-23916

# ── zlib (Debian 12) ─────────────────────────────────────────────────────────
CVE-2023-45853

# ── libgcc / libstdc++ (Debian 12 - sin fix upstream) ───────────────────────
CVE-2022-27943

# ── tar (Debian 12) ──────────────────────────────────────────────────────────
CVE-2005-2541
